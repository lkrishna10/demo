config --global user.name "kkssp"
config --global user.email "lkrishna10@gmail.com"
git config --global -e

git config --global diff.tool p4merge

git config --global difftool.p4merge.path "C:\Program Files\Perforce\p4merge.exe"
git config --global difftool.prompt false

git config --global merge.tool p4merge
git config --global mergetool.p4merge.parth "C:\Program Files\Perforce\p4merge.exe"
git config --global mergetool.prompt false

pwd -- shows the current working directory

ls - al -- list all th folders

mkdir projects    --- createst the folder called projects

cd projects
clear

create a new project in git
git init demo

git status  -- provides the status on the files.

git add README.md  -- add the file to staging

git commit -m "First file commited"

rm -rf .git

git init .

git add .  -- command to add all files to github

git commit -- will invoke the editor for comments and add all files into the commit folder.

git log -- to get the logs for the folder

git show -- all commits and diff, to get out press q

git ls-files -- shows all tracked files

rm readme.md -- remove a file.

git commit -am "comments" --- moving to staging area and commiting the data

git reset HEAD README.md -- reverts to modified stage.

git checkout -- README.md

git help log -- help command

git log --oneline --graph --decorate --all

git config --global alias.hist "log --oneline --graph --decorate --all" -- giving alias name for long commang

git config --global --list - viewing all the global variables

git hist

git hist -- GitHubNotes -- extending the alias name

 git mv renameexample demo -- renaming the file

git rm demo  -- removing the file

git add -A 

git add -u 

git --- .ignore file will help to exclude files from being taken care by git.

git diff 604f404 e87f27e

git difftool  - will launch the visual merge tool

 git branch

  git checkout -b updates  -- creates a new branch

  

